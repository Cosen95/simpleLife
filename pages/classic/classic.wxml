<!--pages/classic/classic.wxml-->
<view class="header">
    <v-epsoide class="epsoide" index="{{index}}" />
    <v-like class="like" bind:like="handleLike" check="{{check}}" count="{{count}}" />
</view>
<v-movie content="{{content}}" image="{{image}}" />


    